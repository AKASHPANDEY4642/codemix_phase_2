# =============================================================================
# MedManglish-RAG: Production Dependencies
# =============================================================================
# Install: pip install -r requirements.txt
# Note: Use faiss-gpu on Ubuntu with CUDA, faiss-cpu on CPU-only systems.

# --- Core ML / NLP ---
torch>=2.1.0
transformers>=4.41.0
tokenizers>=0.19.0
sentencepiece>=0.2.0
protobuf>=4.25.0
datasets>=2.20.0
huggingface_hub>=0.23.2
sentence-transformers>=3.0.0

# --- Quantization & Fine-Tuning ---
bitsandbytes>=0.43.0
peft>=0.11.0
trl>=0.8.0
accelerate>=0.30.0

# --- Retrieval & Indexing ---
rank_bm25>=0.2.2
faiss-cpu>=1.8.0
# For Ubuntu GPU: pip install faiss-gpu instead

# --- Transliteration & Language ---
indic-transliteration>=2.3.0

# --- API Clients ---
groq>=0.9.0
openai>=1.30.0

# --- Web Framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
gradio>=4.0.0

# --- Data Processing ---
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
tqdm>=4.66.0
python-dotenv>=1.0.0
pyyaml>=6.0.0

# --- Evaluation ---
ragas>=0.1.7
langchain-groq>=0.1.3
langchain-huggingface>=0.0.1

# --- Testing ---
pytest>=8.0.0
pytest-cov>=5.0.0